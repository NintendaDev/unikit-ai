---
version: 1.0.0
---

# Code Style (schema2-valid unity fixture)

> **Scope**: Unity core rule used by schema:2 rules-cli smoke tests — fixture content only.
> **Load when**: running rules-cli tests that exercise schema:2 per-module resolution.

---

Fixture content for the unity engine under the schema:2 `code/` layout. Exists so
schema:2 status/migrate/format tests have a non-empty core section to validate.
