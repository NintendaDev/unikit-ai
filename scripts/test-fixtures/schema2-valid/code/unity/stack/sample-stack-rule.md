---
version: 1.0.0
---

# Sample Stack Rule (schema2-valid unity fixture)

> **Scope**: Minimal stack rule used by schema:2 rules-cli smoke tests.
> **Load when**: running rules-cli tests that fetch a schema:2 stack rule with references.
> **References**: references/sample-stack-rule-quickref.md

---

Fixture content for a schema:2 stack rule. Carries a single reference doc so the
schema-aware transport reference path (`code/<engine>/stack/references/`) is exercised.
