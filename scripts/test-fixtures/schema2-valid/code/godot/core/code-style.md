---
version: 1.0.0
---

# Code Style (schema2-valid godot fixture)

> **Scope**: Godot core rule used by schema:2 rules-cli smoke tests — fixture content only.
> **Load when**: running rules-cli tests that exercise `--engine godot` against a schema:2 source.

---

Fixture content for the godot engine under the schema:2 `code/` layout, so
`--engine godot` override tests have a non-empty core section to validate.
