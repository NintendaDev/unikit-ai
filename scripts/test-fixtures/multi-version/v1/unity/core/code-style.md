---
version: 1.0.0
---

# Code Style (multi-version v1)

> **Scope**: Core rule at v1.0.0 used by multi-version smoke tests — synthetic content.
> **Load when**: running rules-cli sync upgrade tests against the multi-version fixture.

---

Fixture content. Any edit here must bump `version:` in both the frontmatter
above and the manifest entry so sync can observe the change.
