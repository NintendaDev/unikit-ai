---
version: 1.0.0
---

# Code Style (minimal-valid godot fixture)

> **Scope**: Godot core rule used by rules-cli smoke tests — fixture content only.
> **Load when**: running rules-cli tests that exercise multi-engine manifest lookups.

---

Fixture content for godot engine. Exists so `--engine godot` override tests
against minimal-valid have a non-empty core section to validate.
