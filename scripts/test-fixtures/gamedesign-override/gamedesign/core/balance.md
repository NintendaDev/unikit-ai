---
version: 9.9.9
---

# Game Balance (STUDIO OVERRIDE)

> **Scope**: This studio's house override of the canonical balance rule — gamedesign-override fixture.
> **Load when**: balancing this studio's games (overrides the canonical balance knowledge).

---

STUDIO OVERRIDE MARKER — this custom content must win over the bundled canonical
balance rule via the per-id B-merge resolver (#R2a): the override id `balance`
resolves from the custom registry (origin `primary`), while every other canonical
gamedesign core id backfills from official/bundled.
