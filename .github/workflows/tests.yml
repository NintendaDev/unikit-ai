name: Tests

on:
  pull_request:
    branches: [main]
  push:
    branches: [main, development]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '22'

      - name: Install dependencies
        run: npm ci

      - name: Download rules repository
        run: npm run download:rules

      - name: Build
        run: npm run build

      - name: Lint
        run: npm run lint

      - name: Validate skills
        run: npm test
